<UserControl x:Class="MonolithGui.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Security Reports &amp; Audit Trail" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <TabControl Grid.Row="1">
            <TabItem Header="Executive PDF Reports">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{StaticResource CardBrush}" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox Grid.Column="0" ItemsSource="{Binding ReportTypes}" SelectedValuePath="Value" DisplayMemberPath="DisplayName" SelectedValue="{Binding SelectedReportType}" Margin="0,0,12,0" Height="28" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="Generate Report" Style="{StaticResource PrimaryButton}" Command="{Binding GenerateReportCommand}" Height="28"/>
                        </Grid>
                    </Border>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Reports}" SelectedItem="{Binding SelectedReport}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Report ID" Binding="{Binding Id}" Width="120"/>
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Format" Binding="{Binding Format}" Width="90"/>
                            <DataGridTextColumn Header="Generated At" Binding="{Binding CreatedAt}" Width="160"/>
                            <DataGridTemplateColumn Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Download PDF" Style="{StaticResource PrimaryButton}" Padding="8,2"
                                                Command="{Binding DataContext.DownloadReportCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Chained Audit Log Trail">
                <Grid Margin="12">
                    <DataGrid ItemsSource="{Binding AuditLogs}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                            <DataGridTextColumn Header="Actor" Binding="{Binding Actor}" Width="120"/>
                            <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="2*"/>
                            <DataGridTextColumn Header="HMAC Hash" Binding="{Binding Hash}" Width="180"/>
                            <DataGridTextColumn Header="Previous Hash" Binding="{Binding PrevHash}" Width="180"/>
                            <DataGridTextColumn Header="Timestamp" Binding="{Binding CreatedAt}" Width="160"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
