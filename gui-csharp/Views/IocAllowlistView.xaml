<UserControl x:Class="MonolithGui.Views.IocAllowlistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Bar -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Indicators of Compromise &amp; Allowlist Exclusions" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource MutedBrush}"/>
            <TextBox Grid.Column="2" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Height="28"/>
        </Grid>

        <!-- Main Tab Control -->
        <TabControl Grid.Row="1">
            <TabItem Header="IoC Threat Rules">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Add IoC Input Form -->
                    <Border Grid.Row="0" Background="{StaticResource CardBrush}" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox Grid.Column="0" SelectedValue="{Binding SelectedIocType}" SelectedValuePath="Content" Margin="0,0,8,0" Height="28" VerticalAlignment="Center">
                                <ComboBoxItem Content="hash"/>
                                <ComboBoxItem Content="ip"/>
                                <ComboBoxItem Content="domain"/>
                            </ComboBox>
                            <TextBox Grid.Column="1" Text="{Binding NewIocValue, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" Height="28" VerticalAlignment="Center" ToolTip="IoC Target Value"/>
                            <TextBox Grid.Column="2" Text="{Binding NewIocDesc, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" Height="28" VerticalAlignment="Center" ToolTip="Rule Description"/>
                            <Button Grid.Column="3" Content="Add IoC" Style="{StaticResource PrimaryButton}" Command="{Binding AddIocCommand}" Height="28"/>
                        </Grid>
                    </Border>

                    <!-- IoCs Table -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Iocs}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="90"/>
                            <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="2*"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="3*"/>
                            <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt}" Width="160"/>
                            <DataGridTemplateColumn Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Delete" Style="{StaticResource DangerButton}" Padding="8,2"
                                                Command="{Binding DataContext.DeleteIocCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Allowlist Exclusions">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Add Allowlist Form -->
                    <Border Grid.Row="0" Background="{StaticResource CardBrush}" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox Grid.Column="0" SelectedValue="{Binding SelectedAllowlistType}" SelectedValuePath="Content" Margin="0,0,8,0" Height="28" VerticalAlignment="Center">
                                <ComboBoxItem Content="path"/>
                                <ComboBoxItem Content="hash"/>
                                <ComboBoxItem Content="cmdline"/>
                            </ComboBox>
                            <TextBox Grid.Column="1" Text="{Binding NewAllowlistTarget, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" Height="28" VerticalAlignment="Center" ToolTip="Excluded Target"/>
                            <Button Grid.Column="2" Content="Add Exclusion" Command="{Binding AddAllowlistCommand}" Height="28"/>
                        </Grid>
                    </Border>

                    <!-- Allowlist Table -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding AllowlistItems}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                            <DataGridTextColumn Header="Target" Binding="{Binding Target}" Width="*"/>
                            <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt}" Width="160"/>
                            <DataGridTemplateColumn Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Remove" Style="{StaticResource DangerButton}" Padding="8,2"
                                                Command="{Binding DataContext.DeleteAllowlistCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
