<UserControl x:Class="MonolithGui.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="180"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="System File &amp; RAM Scanner" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <!-- Scan Action Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Card 1: Quick Scan -->
            <Border Grid.Column="0" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="16" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Quick Scan" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock Text="Scan system RAM, running processes, and critical boot locations." FontSize="11" Foreground="{StaticResource SubtextBrush}" TextWrapping="Wrap" Margin="0,6,0,16" Height="34"/>
                    <Button Content="Run Quick Scan" Style="{StaticResource PrimaryButton}" Command="{Binding QuickScanCommand}" IsEnabled="{Binding IsScanning, Converter={StaticResource InverseBool}}"/>
                </StackPanel>
            </Border>

            <!-- Card 2: Full Scan -->
            <Border Grid.Column="1" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="16" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Full System Scan" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock Text="Scan all partitions, drives, active processes, and memory." FontSize="11" Foreground="{StaticResource SubtextBrush}" TextWrapping="Wrap" Margin="0,6,0,16" Height="34"/>
                    <Button Content="Run Full Scan" Command="{Binding FullScanCommand}" IsEnabled="{Binding IsScanning, Converter={StaticResource InverseBool}}"/>
                </StackPanel>
            </Border>

            <!-- Card 3: Custom Scan -->
            <Border Grid.Column="2" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="16">
                <StackPanel>
                    <TextBlock Text="Custom Target Scan" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock Text="Select specific folder or file paths to inspect for threats." FontSize="11" Foreground="{StaticResource SubtextBrush}" TextWrapping="Wrap" Margin="0,6,0,16" Height="34"/>
                    <Button Content="Select Target..." Command="{Binding CustomScanCommand}" IsEnabled="{Binding IsScanning, Converter={StaticResource InverseBool}}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Progress Bar & Results Tab -->
        <Border Grid.Row="2" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Scan Progress Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,14" Visibility="{Binding IsScanning, Converter={StaticResource BoolToVis}}">
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding ScanStatus}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}"/>
                        <Button Grid.Column="1" Content="Cancel Scan" Style="{StaticResource DangerButton}" Command="{Binding CancelScanCommand}"/>
                    </Grid>
                    <ProgressBar Value="{Binding ScanProgress}" Minimum="0" Maximum="100" Height="10" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding ScanPath}" FontSize="11" Foreground="{StaticResource MutedBrush}"/>
                </StackPanel>

                <!-- Results & History Tabs -->
                <TabControl Grid.Row="1">
                    <TabItem Header="Scan Threat Results">
                        <Grid Margin="8">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                                <DataGrid ItemsSource="{Binding ScanResults}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="File Name" Binding="{Binding Name}" Width="160"/>
                                        <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*"/>
                                        <DataGridTextColumn Header="Verdict" Binding="{Binding Verdict}" Width="100"/>
                                        <DataGridTextColumn Header="Threat Score" Binding="{Binding Score}" Width="100"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </ScrollViewer>
                        </Grid>
                    </TabItem>

                    <TabItem Header="Scan History">
                        <Grid Margin="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Button Grid.Row="0" Content="Refresh History" Command="{Binding RefreshHistoryCommand}" HorizontalAlignment="Right" Margin="0,0,0,8" Width="120"/>
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                                <DataGrid ItemsSource="{Binding ScanHistory}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Scan ID" Binding="{Binding Id}" Width="120"/>
                                        <DataGridTextColumn Header="Scan Type" Binding="{Binding ScanType}" Width="120"/>
                                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                                        <DataGridTextColumn Header="Timestamp" Binding="{Binding CreatedAt}" Width="160"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </ScrollViewer>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Grid>
        </Border>
    </Grid>
</UserControl>
