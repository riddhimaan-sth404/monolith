<UserControl x:Class="MonolithGui.Views.EndpointsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Endpoints &amp; Host Control" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>

            <TextBlock Grid.Column="1" Text="Status:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource MutedBrush}"/>
            <ComboBox Grid.Column="2" SelectedValue="{Binding StatusFilter, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content" Height="28" Margin="0,0,12,0" VerticalAlignment="Center">
                <ComboBoxItem Content="All"/>
                <ComboBoxItem Content="Online"/>
                <ComboBoxItem Content="Offline"/>
                <ComboBoxItem Content="Isolated"/>
            </ComboBox>

            <TextBlock Grid.Column="3" Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource MutedBrush}"/>
            <TextBox Grid.Column="4" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Height="28"/>
        </Grid>

        <!-- Endpoints Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Endpoints}" SelectedItem="{Binding SelectedEndpoint}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Hostname" Binding="{Binding Hostname}" Width="*"/>
                <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="120"/>
                <DataGridTextColumn Header="OS Version" Binding="{Binding OsVersion}" Width="1.5*"/>
                <DataGridTextColumn Header="Agent" Binding="{Binding AgentVersion}" Width="90"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90"/>
                <DataGridTextColumn Header="Last Seen" Binding="{Binding LastSeen}" Width="160"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Row -->
        <Grid Grid.Row="2" Margin="0,12,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Refresh List" Command="{Binding RefreshCommand}"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="Shred Remote File..." Margin="0,0,8,0" Command="{Binding ShredFileCommand}" IsEnabled="{Binding IsEndpointSelected}"/>
                <Button Content="Isolate Host" Style="{StaticResource DangerButton}" Margin="0,0,8,0" Command="{Binding IsolateCommand}" IsEnabled="{Binding IsEndpointSelected}"/>
                <Button Content="Release Host" Style="{StaticResource PrimaryButton}" Command="{Binding ReleaseCommand}" IsEnabled="{Binding IsEndpointSelected}"/>
            </StackPanel>
        </Grid>

        <!-- Host Events Drawer -->
        <Border Grid.Row="3" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Endpoint Events Drawer" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,8"/>
                <ListBox Grid.Row="1" ItemsSource="{Binding HostEvents}" Background="Transparent" BorderThickness="0" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextBrush}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
