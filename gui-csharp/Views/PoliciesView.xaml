<UserControl x:Class="MonolithGui.Views.PoliciesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="220"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Security Policies &amp; Endpoints Configuration" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <!-- Top Panel: Form to create policies and assignment bar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Create Policy Card -->
            <Border Grid.Column="0" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="16" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Create Security Policy Profile" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,10"/>
                    
                    <TextBox Text="{Binding NewPolicyName, UpdateSourceTrigger=PropertyChanged}" ToolTip="Policy Name" Margin="0,0,0,8" Height="28"/>
                    
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Quick Scan Interval (hours):" FontSize="11" Foreground="{StaticResource SubtextBrush}" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding ScanInterval, UpdateSourceTrigger=PropertyChanged}" Width="60" Height="26"/>
                    </Grid>

                    <CheckBox Content="Enable Real-Time RAM Memory Scanner" IsChecked="{Binding MemoryScanEnabled}" Margin="0,0,0,8"/>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Tamper Protection Level:" FontSize="11" Foreground="{StaticResource SubtextBrush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" SelectedValue="{Binding TamperLevel, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content" Width="80" Height="26">
                            <ComboBoxItem Content="low"/>
                            <ComboBoxItem Content="medium"/>
                            <ComboBoxItem Content="high"/>
                        </ComboBox>
                    </Grid>

                    <Button Content="Create Policy Profile" Style="{StaticResource PrimaryButton}" Command="{Binding CreatePolicyCommand}"/>
                </StackPanel>
            </Border>

            <!-- Assign Policy Card -->
            <Border Grid.Column="1" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="16">
                <StackPanel>
                    <TextBlock Text="Assign Policy to Endpoint" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,10"/>
                    <TextBlock Text="Select target Policy and target Endpoint to push configuration:" FontSize="11" Foreground="{StaticResource SubtextBrush}" Margin="0,0,0,8"/>

                    <ComboBox ItemsSource="{Binding Policies}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedPolicy}" Margin="0,0,0,8" Height="28"/>
                    <ComboBox ItemsSource="{Binding Endpoints}" DisplayMemberPath="Hostname" SelectedItem="{Binding SelectedEndpoint}" Margin="0,0,0,12" Height="28"/>

                    <Button Content="Assign Policy Profile" Command="{Binding AssignPolicyCommand}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Bottom Panel: Policies Table -->
        <Border Grid.Row="2" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Active Policy Profiles" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshCommand}" Padding="8,2" FontSize="11" VerticalAlignment="Center"/>
                </Grid>
                <DataGrid Grid.Row="1" ItemsSource="{Binding Policies}" SelectedItem="{Binding SelectedPolicy}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Policy Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Scan Interval (hrs)" Binding="{Binding QuickScanIntervalHours}" Width="140"/>
                        <DataGridTextColumn Header="RAM Scanning" Binding="{Binding MemoryScanEnabled}" Width="120"/>
                        <DataGridTextColumn Header="Tamper Level" Binding="{Binding TamperProtectionLevel}" Width="120"/>
                        <DataGridTextColumn Header="Assigned Hosts" Binding="{Binding AssignedEndpointsCount}" Width="110"/>
                        <DataGridTemplateColumn Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Delete" Style="{StaticResource DangerButton}" Padding="8,2"
                                            Command="{Binding DataContext.DeletePolicyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
